---
# FIXME: 所有者がrootのままのため。修正する
prometheus_system_group: 'root'
prometheus_scrape_configs:
  - job_name: "prometheus"
    metrics_path: "{{ prometheus_metrics_path }}"
    static_configs:
      - targets:
          - "{{ ansible_fqdn | default(ansible_host) | default('localhost') }}:9090"
  - job_name: "grafana"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["localhost:3000"]
  # インフラノードのホストメトリクス(node_exporter)
  # ※ K3sノードはkube-prometheus-stack側で収集するため対象外
  - job_name: "node"
    static_configs:
      - targets:
          - "192.168.0.210:9100" # openmediavault
          - "192.168.0.212:9100" # vault
          - "192.168.0.214:9100" # monitoring
  # NASのディスクSMART(smartctl_exporter)
  - job_name: "smartctl"
    static_configs:
      - targets:
          - "192.168.0.210:9633" # openmediavault
  # 外形監視(blackbox_exporter): HTTP疎通
  - job_name: "blackbox-http"
    metrics_path: /probe
    params:
      module: [http_2xx]
    static_configs:
      - targets:
          - http://192.168.0.214:3000 # grafana
          - http://192.168.0.231/admin # pi-hole
          - http://192.168.0.235 # homepage
          - http://192.168.0.232:9090 # k3s prometheus
    relabel_configs:
      - source_labels: [__address__]
        target_label: __param_target
      - source_labels: [__param_target]
        target_label: instance
      - target_label: __address__
        replacement: 192.168.0.214:9115
  # 外形監視(blackbox_exporter): ICMP疎通
  - job_name: "blackbox-icmp"
    metrics_path: /probe
    params:
      module: [icmp]
    static_configs:
      - targets:
          - 192.168.0.210 # openmediavault
          - 192.168.0.212 # vault
    relabel_configs:
      - source_labels: [__address__]
        target_label: __param_target
      - source_labels: [__param_target]
        target_label: instance
      - target_label: __address__
        replacement: 192.168.0.214:9115
