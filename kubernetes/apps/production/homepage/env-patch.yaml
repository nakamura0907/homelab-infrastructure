---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: homepage
  namespace: homepage
spec:
  values:
    env:
      # Ingress の Host。これが合わないと Homepage が Host ヘッダを拒否する
      - name: HOMEPAGE_ALLOWED_HOSTS
        value: "homepage.home.arpa"
